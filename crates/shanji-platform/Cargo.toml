[package]
name = "shanji-platform"
version = "0.1.0"
edition = "2021"

[dependencies]
global-hotkey = "0.6"
serde = { version = "1", features = ["derive"] }
shanji-core = { path = "../shanji-core" }
tray-icon = "0.21"
